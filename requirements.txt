# Core ML
torch>=2.0.0  # Apple Silicon friendly: torch can use MPS
transformers>=4.40.0
datasets>=2.0.0
peft>=0.10.0

# API server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Testing
pytest>=7.0.0

# Upgrading torch on Apple Silicon:
# pip install --pre torch --index-url https://download.pytorch.org/whl/nightly/cpu
# Then ensure torch.backends.mps.is_available() == True
# For PEFT: pip install peft
